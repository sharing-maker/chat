import { useImageCollection } from "../../hooks/collection/useImageCollection";

const ImageCollection = () => {
  const {} = useImageCollection();
  return <div></div>;
};

export default ImageCollection;
