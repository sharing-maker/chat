const conversationBg = new URL(
  "../assets/images/conversationBg.png",
  import.meta.url
).href;
const imageFailed = new URL("../assets/images/imageFailed.png", import.meta.url)
  .href;

export const images = {
  conversationBg,
  imageFailed,
};
